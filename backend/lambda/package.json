{
  "name": "quickshare-lambda",
  "version": "1.0.0",
  "main": "lambda/index.js",
  "scripts": {
    "package": "cd .. && powershell Compress-Archive -Path controllers,middleware,models,routes,utils,lambda,package.json,package-lock.json -DestinationPath lambda-deploy.zip -Force"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "ioredis": "^5.11.1",
    "mongoose": "^9.7.3",
    "nanoid": "^3.3.15",
    "serverless-http": "^3.2.0"
  }
}
